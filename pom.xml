<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="
         http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.14</version>
        <relativePath/>
    </parent>

    <groupId>com.everypicfound</groupId>
    <artifactId>everypicfound-parent</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>EveryPicFound Parent</name>

    <modules>
        <module>security-contract</module>
        <module>interaction-service</module>
        <module>identity-service</module>
        <module>media-search-service</module>
        <module>gateway-service</module>
    </modules>

    <properties>
        <java.version>17</java.version>
         <maven.compiler.release>${java.version}</maven.compiler.release>

        <!-- Spring Cloud 与 Spring Boot 3.5.x 的兼容版本 -->
        <spring-cloud.version>2025.0.3</spring-cloud.version>

        <!-- Spring Boot BOM 未管理或需要明确锁定的依赖 -->
        <mybatis-plus.version>3.5.12</mybatis-plus.version>
        <qdrant-client.version>1.18.1</qdrant-client.version>

        <error-prone-annotations.version>
            2.41.0
        </error-prone-annotations.version>

        <!-- 构建工具 -->
        <maven.minimum.version>3.9.10</maven.minimum.version>
        <maven-enforcer-plugin.version>
            3.6.3
        </maven-enforcer-plugin.version>

        <project.build.sourceEncoding>
            UTF-8
        </project.build.sourceEncoding>
        <project.reporting.outputEncoding>
            UTF-8
        </project.reporting.outputEncoding>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Cloud 统一版本管理 -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- 项目内部契约模块 -->
            <dependency>
                <groupId>com.everypicfound</groupId>
                <artifactId>security-contract</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- 当前 Spring Boot BOM 未管理的第三方依赖 -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>

            <dependency>
                <groupId>com.google.errorprone</groupId>
                <artifactId>error_prone_annotations</artifactId>
                <version>${error-prone-annotations.version}</version>
            </dependency>

            <dependency>
                <groupId>io.qdrant</groupId>
                <artifactId>client</artifactId>
                <version>${qdrant-client.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                </plugin>

                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                        <parameters>true</parameters>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>
                    ${maven-enforcer-plugin.version}
                </version>

                <executions>
                    <execution>
                        <id>enforce-project-baseline</id>
                        <phase>validate</phase>

                        <goals>
                            <goal>enforce</goal>
                        </goals>

                        <configuration>
                            <fail>true</fail>

                            <rules>
                                <requireMavenVersion>
                                    <version>
                                        [${maven.minimum.version},4.0.0)
                                    </version>
                                    <message>
                                        EveryPicFound requires Maven
                                        ${maven.minimum.version} or later.
                                    </message>
                                </requireMavenVersion>

                                <requireJavaVersion>
                                    <version>[17,)</version>
                                    <display>true</display>
                                    <message>
                                        EveryPicFound requires JDK 17
                                        or later to build.
                                    </message>
                                </requireJavaVersion>

                                <reactorModuleConvergence>
                                    <message>
                                        EveryPicFound reactor modules
                                        must use the same parent and
                                        project version.
                                    </message>
                                </reactorModuleConvergence>

                                <banDuplicatePomDependencyVersions/>

                                <dependencyConvergence>
                                    <uniqueVersions>false</uniqueVersions>
                                </dependencyConvergence>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>